// Challenge 1 — PokemonCard component
//
// Create a reusable PokemonCard component.
//
// Props:
//   - name (string): the Pokémon name to display
//
// Requirements:
//   - The card must be clickable (use the onClick prop).
//   - When this Pokémon is selected (isSelected prop is true),
//     give the card a visually distinct background colour.
//
// Props summary:
//   name       string   — Pokémon name
//   onClick    function — called when the card is clicked
//   isSelected boolean  — whether this card is currently selected

export function PokemonCard({ name, onClick, isSelected }) {
  // TODO: implement
}
